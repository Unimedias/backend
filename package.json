{
  "name": "json_server",
  "version": "1.0.0",
  "description": "Fake REST API for testing",
  "main": "index.js",
  "scripts": {
    "generate-mock-data": "node generateMockData",
    "prestart-mockapi": "npm run generate-mock-data",
    "start-mockapi": "json-server --watch db.json"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "json-schema-faker": "^0.5.0-rc22",
    "json-server": "^0.15.1"
  }
}
